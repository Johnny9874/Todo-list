RewriteEngine On

# Permet d'exécuter directement les fichiers PHP s'ils existent
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^(.*)$ $1 [QSA,L]

# Redirection par défaut vers login.html
RewriteRule ^$ /html/login.html [L]
