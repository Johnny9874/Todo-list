RewriteEngine On

# Assurez-vous que les fichiers PHP sont accessibles directement
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Si ce n'est pas un fichier ou un dossier existant, redirige vers index.php
RewriteRule ^ index.php [L]
