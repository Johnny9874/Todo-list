RewriteEngine On

# Permet d'exécuter les fichiers PHP normalement s'ils existent
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^(.*)$ $1 [QSA,L]

# Activer la réécriture des URL
Options +FollowSymLinks
