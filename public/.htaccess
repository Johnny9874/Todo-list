RewriteEngine On

# Si un fichier ou un dossier existe, le servir directement
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Rediriger toutes les requêtes vers index.php
RewriteRule ^ index.php [L]

